from django.apps import AppConfig

class RailwayReservationSystemConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'railway_reservation_system'  # Must match your app folder name